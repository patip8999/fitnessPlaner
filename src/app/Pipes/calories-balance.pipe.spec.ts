import { CaloriesBalancePipe } from './calories-balance.pipe';

describe('CaloriesBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new CaloriesBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
